# AI 분석 → 맞춤 추천 연결 개선 스펙

> **Phase**: Launch Enhancement
> **Version**: 1.0
> **Created**: 2026-01-01
> **Status**: ✅ Implemented (2026-01-01)

## 1. 개요

### 1.1 현재 문제점

각 AI 분석(체형, 피부, 퍼스널 컬러) 결과 화면에서 **다음 액션으로의 자연스러운 전환**이 부족함.

| 분석        | 현재 버튼          | 문제                         |
| ----------- | ------------------ | ---------------------------- |
| 체형 분석   | 홈으로 / 다시 분석 | 운동 추천 연결 없음          |
| 피부 분석   | 홈으로 / 다시 분석 | 제품 추천 연결 없음          |
| 퍼스널 컬러 | 홈으로 / 다시 분석 | 코디/메이크업 추천 연결 없음 |

### 1.2 목표

- 분석 결과 → 맞춤 추천의 **자연스러운 사용자 플로우** 구현
- 분석 데이터를 활용한 **개인화된 추천** 제공
- 사용자 전환율 향상 (분석 완료 → 제품/운동 시작)

## 2. 요구사항

### 2.1 기능 요구사항

#### FR-1: 체형 분석 결과 → 운동 추천 연결

- **[FR-1.1]** 체형 분석 결과 화면에 "나에게 맞는 운동 추천" 버튼 추가
- **[FR-1.2]** 버튼 클릭 시 운동 온보딩(`/(workout)/onboarding`) 또는 결과 화면으로 이동
- **[FR-1.3]** 체형 정보(bodyType, BMI)를 쿼리 파라미터로 전달

#### FR-2: 피부 분석 결과 → 스킨케어 추천 연결

- **[FR-2.1]** 피부 분석 결과 화면에 "피부 타입 맞춤 제품 보기" 버튼 추가
- **[FR-2.2]** 버튼 클릭 시 제품 목록(`/products`)으로 이동
- **[FR-2.3]** 피부 타입(skinType), 고민(concerns)을 쿼리 파라미터로 전달
- **[FR-2.4]** 제품 목록에서 피부 타입 기반 필터링 지원

#### FR-3: 퍼스널 컬러 → 메이크업/코디 추천 연결

- **[FR-3.1]** 퍼스널 컬러 결과 화면에 "내 색상에 맞는 제품 보기" 버튼 추가
- **[FR-3.2]** 버튼 클릭 시 제품 목록(`/products`)으로 이동
- **[FR-3.3]** 시즌(season), 톤(tone)을 쿼리 파라미터로 전달
- **[FR-3.4]** 제품 목록에서 메이크업 카테고리 자동 선택

#### FR-4: 제품 목록 쿼리 파라미터 지원

- **[FR-4.1]** `/products?skinType=dry&category=skincare` 형식 지원
- **[FR-4.2]** `/products?season=Spring&category=makeup` 형식 지원
- **[FR-4.3]** 파라미터에 따른 자동 필터링 및 정렬

### 2.2 비기능 요구사항

- **[NFR-1]** 버튼 클릭 시 Haptic 피드백 제공
- **[NFR-2]** 다크 모드 지원
- **[NFR-3]** 접근성: 버튼에 적절한 레이블 제공

## 3. UI/UX 설계

### 3.1 분석 결과 화면 레이아웃

```
┌────────────────────────────────────┐
│         [분석 결과 내용]            │
│                                    │
│  ┌──────────────────────────────┐  │
│  │   🎯 맞춤 추천 받기 (Primary)  │  │  ← 신규 추가
│  └──────────────────────────────┘  │
│                                    │
│  ┌──────────────────────────────┐  │
│  │   홈으로 돌아가기 (Secondary)  │  │
│  └──────────────────────────────┘  │
│                                    │
│  [다시 분석하기 링크]               │
└────────────────────────────────────┘
```

### 3.2 버튼 텍스트

| 분석        | Primary 버튼            | 아이콘 |
| ----------- | ----------------------- | ------ |
| 체형        | "나에게 맞는 운동 추천" | 🏃     |
| 피부        | "피부 맞춤 제품 보기"   | 🧴     |
| 퍼스널 컬러 | "내 색상에 맞는 제품"   | 💄     |

### 3.3 색상

- Primary 버튼: `#2e5afa` (이룸 브랜드 블루)
- Secondary 버튼: `#f5f5f5` (라이트) / `#2a2a2a` (다크)

## 4. 데이터 플로우

### 4.1 쿼리 파라미터 스키마

```typescript
// 제품 목록 쿼리 파라미터
interface ProductQueryParams {
  // 피부 분석 기반
  skinType?: 'dry' | 'oily' | 'combination' | 'sensitive' | 'normal';
  concerns?: string; // 콤마 구분 (예: "acne,wrinkles")

  // 퍼스널 컬러 기반
  season?: 'Spring' | 'Summer' | 'Autumn' | 'Winter';
  tone?: 'warm' | 'cool';

  // 공통
  category?: 'skincare' | 'makeup' | 'supplement' | 'equipment';
}

// 운동 온보딩 쿼리 파라미터
interface WorkoutQueryParams {
  bodyType?: string;
  bmi?: number;
  fromAnalysis?: 'body'; // 체형 분석에서 왔음을 표시
}
```

### 4.2 데이터 전달 흐름

```
[체형 분석 결과]
    │
    ├── bodyType: "hourglass"
    ├── bmi: 22.5
    │
    ▼
/(workout)/onboarding?bodyType=hourglass&bmi=22.5&fromAnalysis=body

[피부 분석 결과]
    │
    ├── skinType: "combination"
    ├── concerns: ["pores", "oiliness"]
    │
    ▼
/products?skinType=combination&concerns=pores,oiliness&category=skincare

[퍼스널 컬러 결과]
    │
    ├── season: "Spring"
    ├── tone: "warm"
    │
    ▼
/products?season=Spring&tone=warm&category=makeup
```

## 5. 수정 파일

### 5.1 분석 결과 화면 (3개)

| 파일                                                   | 변경 사항           |
| ------------------------------------------------------ | ------------------- |
| `apps/mobile/app/(analysis)/body/result.tsx`           | 운동 추천 버튼 추가 |
| `apps/mobile/app/(analysis)/skin/result.tsx`           | 제품 추천 버튼 추가 |
| `apps/mobile/app/(analysis)/personal-color/result.tsx` | 제품 추천 버튼 추가 |

### 5.2 제품 목록 화면 (1개)

| 파일                                 | 변경 사항                              |
| ------------------------------------ | -------------------------------------- |
| `apps/mobile/app/products/index.tsx` | 쿼리 파라미터 파싱 및 필터링 로직 추가 |

### 5.3 운동 온보딩 화면 (선택)

| 파일                                             | 변경 사항                  |
| ------------------------------------------------ | -------------------------- |
| `apps/mobile/app/(workout)/onboarding/index.tsx` | 체형 분석 결과 활용 (선택) |

## 6. 테스트 시나리오

### 6.1 체형 분석 → 운동 추천

1. 체형 분석 완료
2. "나에게 맞는 운동 추천" 버튼 탭
3. 운동 온보딩 화면으로 이동 확인
4. 체형 정보가 쿼리 파라미터로 전달됨 확인

### 6.2 피부 분석 → 제품 추천

1. 피부 분석 완료
2. "피부 맞춤 제품 보기" 버튼 탭
3. 제품 목록 화면으로 이동 확인
4. 스킨케어 카테고리 자동 선택 확인
5. 피부 타입 기반 필터링 동작 확인

### 6.3 퍼스널 컬러 → 메이크업 추천

1. 퍼스널 컬러 분석 완료
2. "내 색상에 맞는 제품" 버튼 탭
3. 제품 목록 화면으로 이동 확인
4. 메이크업 카테고리 자동 선택 확인
5. 시즌 기반 추천 배너 표시 확인

## 7. 마일스톤

| 단계 | 작업                                | 예상 |
| ---- | ----------------------------------- | ---- |
| 1    | 분석 결과 화면 버튼 추가 (3개 파일) | 간단 |
| 2    | 제품 목록 쿼리 파라미터 지원        | 중간 |
| 3    | 테스트 및 검증                      | 간단 |

---

**Approved by**: (승인 대기)
**Reviewed by**: (리뷰 대기)
