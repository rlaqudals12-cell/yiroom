# R-1 주간/월간 리포트 기능 구현 계획 v1.0

**작성일**: 2025-12-03
**버전**: v1.0
**상태**: Plan Mode (사용자 승인 대기)

---

## 1. 기능 개요

### 1.1 목표
W-1 운동 + N-1 영양 데이터를 통합하여 사용자에게 주간/월간 리포트를 제공하는 기능

### 1.2 핵심 가치
```yaml
사용자 가치:
  - 일주일/한달간의 건강 습관 종합 리뷰
  - 목표 달성률 시각화
  - AI 기반 개인화 인사이트
  - 다음 기간 개선 방향 제시

비즈니스 가치:
  - 리텐션 증가: 주간 리포트 알림 → 재방문
  - 프리미엄 전환: 상세 분석 기능
  - 데이터 기반 사용자 참여 유도
```

### 1.3 기존 자산 활용
```yaml
재사용 가능:
  - /api/nutrition/history: 날짜 범위 조회 API
  - /api/nutrition/summary/daily: 일일 요약 로직
  - lib/nutrition/streak.ts: 스트릭 계산
  - lib/nutrition/*Insight.ts: 크로스 모듈 인사이트
  - Dashboard 컴포넌트: ProgressBar, Card 등

신규 개발 필요:
  - 주간/월간 집계 API
  - 트렌드 차트 컴포넌트
  - 리포트 페이지 UI
  - 기간별 인사이트 생성 로직
```

---

## 2. 데이터 구조 설계

### 2.1 주간 리포트 데이터 모델
```typescript
interface WeeklyReport {
  // 메타데이터
  weekStart: string;         // YYYY-MM-DD (월요일)
  weekEnd: string;           // YYYY-MM-DD (일요일)
  generatedAt: string;       // ISO timestamp

  // 영양 요약
  nutrition: {
    summary: {
      totalCalories: number;
      avgCaloriesPerDay: number;
      totalWater: number;
      avgWaterPerDay: number;
      mealCount: number;
      daysWithRecords: number;
    };
    achievement: {
      caloriesPercent: number;    // 평균 목표 대비
      proteinPercent: number;
      waterPercent: number;
    };
    trend: {
      caloriesTrend: 'up' | 'down' | 'stable';
      waterTrend: 'up' | 'down' | 'stable';
      foodQualityScore: number;  // 0-100
    };
    dailyBreakdown: DailyNutrition[];
  };

  // 운동 요약 (workout_logs 테이블 필요)
  workout: {
    summary: {
      totalSessions: number;
      totalDuration: number;      // minutes
      totalCaloriesBurned: number;
      daysWithWorkout: number;
    };
    trend: {
      consistencyScore: number;  // 0-100
      averageIntensity: string;  // light/moderate/intense
    };
  };

  // 통합 인사이트
  insights: {
    highlights: string[];        // 긍정적 변화
    improvements: string[];      // 개선 필요 사항
    tips: string[];              // 다음 주 추천
  };

  // 스트릭 정보
  streak: {
    nutrition: { current: number; milestone: number | null };
    workout: { current: number; milestone: number | null };
  };
}
```

### 2.2 월간 리포트 데이터 모델
```typescript
interface MonthlyReport extends WeeklyReport {
  // 확장 필드
  month: string;              // YYYY-MM

  // 주간 비교
  weeklyComparison: {
    week1: WeeklySummary;
    week2: WeeklySummary;
    week3: WeeklySummary;
    week4: WeeklySummary;
  };

  // 체중 변화 (C-1 연동)
  bodyProgress: {
    startWeight: number | null;
    endWeight: number | null;
    weightChange: number;
    reanalysisRecommended: boolean;
  };

  // 목표 진행률
  goalProgress: {
    goal: string;             // weight_loss, muscle_gain, etc.
    achievementRate: number;  // 0-100
    message: string;
  };
}
```

---

## 3. API 설계

### 3.1 주간 리포트 API
```yaml
Endpoint: GET /api/reports/weekly
Query Params:
  - weekStart: YYYY-MM-DD (선택, 기본값: 이번 주 월요일)

Response:
  - 200: WeeklyReport
  - 401: Unauthorized
  - 404: No data for the period
```

### 3.2 월간 리포트 API
```yaml
Endpoint: GET /api/reports/monthly
Query Params:
  - month: YYYY-MM (선택, 기본값: 이번 달)

Response:
  - 200: MonthlyReport
  - 401: Unauthorized
  - 404: No data for the period
```

### 3.3 리포트 목록 API
```yaml
Endpoint: GET /api/reports
Query Params:
  - type: 'weekly' | 'monthly'
  - limit: number (기본값: 12)

Response:
  - 200: { reports: ReportSummary[] }
```

---

## 4. UI 설계

### 4.1 페이지 구조
```
app/(main)/reports/
├── page.tsx                    # 리포트 목록 (주간/월간 탭)
├── weekly/
│   └── [weekStart]/
│       └── page.tsx            # 주간 리포트 상세
└── monthly/
    └── [month]/
        └── page.tsx            # 월간 리포트 상세
```

### 4.2 컴포넌트 구조
```
components/reports/
├── index.ts                    # exports
├── ReportCard.tsx              # 리포트 목록 카드
├── ReportHeader.tsx            # 기간 + 네비게이션
├── NutritionSummary.tsx        # 영양 요약 카드
├── WorkoutSummary.tsx          # 운동 요약 카드
├── TrendChart.tsx              # 트렌드 차트 (recharts)
├── InsightCard.tsx             # AI 인사이트 카드
├── StreakBadge.tsx             # 스트릭 배지
├── GoalProgressRing.tsx        # 목표 달성률 원형 차트
└── WeeklyComparison.tsx        # 주간 비교 (월간용)
```

### 4.3 UI 와이어프레임

#### 주간 리포트 레이아웃
```
┌─────────────────────────────────────────┐
│  < 이전 주    12/2 - 12/8    다음 주 >   │
├─────────────────────────────────────────┤
│  ┌─────────────┐  ┌─────────────┐       │
│  │ 영양 요약   │  │ 운동 요약   │       │
│  │ 🍽️ 21식    │  │ 🏃 5회      │       │
│  │ 14,200kcal │  │ 2,100kcal  │       │
│  │ 목표 92%   │  │ 소모        │       │
│  └─────────────┘  └─────────────┘       │
├─────────────────────────────────────────┤
│  📊 칼로리 트렌드                        │
│  ┌─────────────────────────────────┐    │
│  │ [일일 칼로리 라인 차트]          │    │
│  └─────────────────────────────────┘    │
├─────────────────────────────────────────┤
│  💡 이번 주 인사이트                     │
│  ┌─────────────────────────────────┐    │
│  │ ✅ 단백질 섭취 목표 110% 달성!   │    │
│  │ ⚠️ 수분 섭취가 목표 대비 부족    │    │
│  │ 💪 운동 5회로 스트릭 유지 중     │    │
│  └─────────────────────────────────┘    │
├─────────────────────────────────────────┤
│  🔥 스트릭                               │
│  영양 기록: 14일 연속 🏆                 │
│  운동 기록: 7일 연속 🔥                  │
└─────────────────────────────────────────┘
```

---

## 5. 구현 계획

### Sprint 구성: 2 Sprints (총 10 Tasks)

### Sprint 1: 기본 인프라 + 주간 리포트 (5 Tasks)

| Task ID | Task 이름 | 설명 | 예상 |
|---------|----------|------|------|
| R-1.1 | 리포트 타입 정의 | WeeklyReport, MonthlyReport 타입 | 0.5h |
| R-1.2 | 주간 집계 로직 | lib/reports/weeklyAggregator.ts | 2h |
| R-1.3 | 주간 리포트 API | /api/reports/weekly | 1.5h |
| R-1.4 | 주간 리포트 UI | 페이지 + 컴포넌트 | 3h |
| R-1.5 | 트렌드 차트 | TrendChart 컴포넌트 (recharts) | 2h |

### Sprint 2: 월간 리포트 + 고도화 (5 Tasks)

| Task ID | Task 이름 | 설명 | 예상 |
|---------|----------|------|------|
| R-2.1 | 월간 집계 로직 | lib/reports/monthlyAggregator.ts | 2h |
| R-2.2 | 월간 리포트 API | /api/reports/monthly | 1.5h |
| R-2.3 | 월간 리포트 UI | 페이지 + 주간 비교 컴포넌트 | 3h |
| R-2.4 | AI 인사이트 생성 | lib/reports/insightGenerator.ts | 2h |
| R-2.5 | 리포트 목록 페이지 | /reports 메인 페이지 | 2h |

---

## 6. 테스트 케이스

### 6.1 주간 리포트 테스트
```yaml
TC-R1-001: 데이터 없는 주간 리포트 요청
TC-R1-002: 일부 날짜만 데이터 있는 경우
TC-R1-003: 전체 7일 데이터 있는 경우
TC-R1-004: 칼로리 트렌드 계산 정확성
TC-R1-005: 목표 달성률 계산
TC-R1-006: 주간 이동 (이전/다음)
```

### 6.2 월간 리포트 테스트
```yaml
TC-R1-010: 주간 비교 데이터 정확성
TC-R1-011: 체중 변화 계산 (C-1 연동)
TC-R1-012: 월간 인사이트 생성
TC-R1-013: 목표 진행률 계산
```

---

## 7. 기술적 고려사항

### 7.1 성능 최적화
```yaml
캐싱 전략:
  - 주간 리포트: 해당 주 종료 후 캐싱
  - 월간 리포트: 해당 월 종료 후 캐싱
  - 현재 주/월: 실시간 계산

데이터베이스:
  - meal_date, clerk_user_id 복합 인덱스 활용
  - 날짜 범위 쿼리 최적화
```

### 7.2 워크아웃 로그 부재 대응
```yaml
현재 상황:
  - workout_logs 테이블 스키마 있음
  - 실제 데이터 기록 기능 미구현

대응 전략:
  - 운동 요약: "데이터 없음" 표시
  - 향후 W-1 운동 기록 기능 추가 시 자동 연동
  - workout.summary.totalSessions = 0 기본값
```

### 7.3 차트 라이브러리
```yaml
선택: recharts
이유:
  - React 네이티브 지원
  - 가벼운 번들 사이즈
  - 반응형 지원
  - 기존 프로젝트 호환성

대안:
  - Nivo: 더 많은 차트 타입
  - Victory: 모바일 친화적
```

---

## 8. Phase 3 연계

### 8.1 확장 가능성
```yaml
프리미엄 기능:
  - PDF 리포트 다운로드
  - 상세 영양소 분석 (비타민, 미네랄)
  - 동기 비교 (나이대/성별 평균)
  - AI 코칭 메시지

대시보드 통합:
  - 메인 대시보드에 미니 리포트 위젯
  - 주간 하이라이트 알림
```

### 8.2 다음 기능 후보
```yaml
1순위: 운동 기록 시스템 (workout_logs 활성화)
2순위: 알림/리마인더 시스템
3순위: 바코드 스캔 음식 등록
4순위: 소셜 공유 기능
```

---

## 9. 예상 일정

```yaml
Sprint 1 (주간 리포트): 1-2일
Sprint 2 (월간 리포트): 1-2일
테스트 + QA: 0.5일
───────────────────────
총 예상: 2.5-4.5일
```

---

## 10. 승인 요청 사항

다음 사항에 대한 승인을 요청합니다:

1. **구현 범위**: Sprint 1-2 (10 Tasks)
2. **차트 라이브러리**: recharts 사용
3. **운동 데이터**: workout_logs 없이 영양 중심 리포트로 시작
4. **진행 순서**: 주간 리포트 → 월간 리포트

---

**문서 버전**: v1.0
**최종 수정일**: 2025-12-03
