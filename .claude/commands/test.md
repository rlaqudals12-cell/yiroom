---
description: 테스트 실행 및 결과 분석
---

# /test 명령어

테스트 실행 및 결과 분석을 수행합니다.

## 연동 에이전트

- **yiroom-test-writer**: 테스트 코드 작성 전문가

---

## 실행 워크플로우

```mermaid
graph TD
    A[/test 호출] --> B{인자 있음?}
    B -->|Yes| C[특정 테스트 실행]
    B -->|No| D[전체 테스트 실행]
    C --> E[결과 분석]
    D --> E
    E -->|실패| F[원인 분석]
    E -->|성공| G[커버리지 확인]
    F --> H{자동 수정?}
    H -->|Yes| I[수정 시도]
    H -->|No| J[수정 방안 제시]
    I --> K{성공?}
    K -->|Yes| G
    K -->|No| L{재시도 < 3?}
    L -->|Yes| I
    L -->|No| J
    G --> M[결과 보고]
```

---

## 테스트 유형

### 단위 테스트 (Unit)

| 대상     | 위치                | 패턴           |
| -------- | ------------------- | -------------- |
| 유틸리티 | `tests/lib/`        | `*.test.ts`    |
| 훅       | `tests/hooks/`      | `use*.test.ts` |
| 컴포넌트 | `tests/components/` | `*.test.tsx`   |

```bash
/test lib/utils/format      # 특정 유틸리티
/test hooks/useAuth         # 특정 훅
```

### API 테스트 (Integration)

| 대상       | 위치              | 패턴            |
| ---------- | ----------------- | --------------- |
| API 라우트 | `tests/api/`      | `route.test.ts` |
| 서비스     | `tests/services/` | `*.test.ts`     |

```bash
/test api/analyze/skin      # 피부 분석 API
/test api/coach/            # 코치 API 전체
```

### E2E 테스트 (Playwright)

```bash
/test e2e/analysis          # 분석 플로우
/test e2e/onboarding        # 온보딩 플로우
```

---

## 커버리지 목표

| 영역            | 목표 | 최소 |
| --------------- | ---- | ---- |
| **lib/**        | 90%  | 80%  |
| **components/** | 75%  | 60%  |
| **api/**        | 85%  | 70%  |
| **전체**        | 80%  | 70%  |

### 커버리지 확인

```bash
/test --coverage            # 전체 커버리지
/test lib/ --coverage       # lib 커버리지
```

---

## 실행 내용

### 1. 테스트 실행

```bash
# 기본 실행
npm run test -- $ARGUMENTS

# 관련 테스트만 (변경 파일 기반)
npm run test -- --related

# 감시 모드
npm run test -- --watch
```

### 2. 실패 분석

| 실패 유형   | 분석 방법           | 자동 수정 |
| ----------- | ------------------- | --------- |
| 타입 오류   | 타입 불일치 확인    | ✅ 가능   |
| Mock 오류   | Mock 데이터 확인    | ✅ 가능   |
| 로직 오류   | 기대값 비교         | ⚠️ 제한적 |
| 비동기 오류 | await/타임아웃 확인 | ✅ 가능   |

### 3. 자동 수정 시도

- 최대 **3회** 재시도
- 각 시도 후 재테스트
- 수정 불가 시 상세 원인 제시

---

## 출력 형식

```markdown
## 🧪 테스트 결과

### 실행 요약

| 항목      | 수  |
| --------- | --- |
| 총 테스트 | X개 |
| 통과 ✅   | X개 |
| 실패 ❌   | X개 |
| 스킵 ⏭️   | X개 |

### 실패 테스트 분석

| 테스트     | 원인   | 수정 방안 |
| ---------- | ------ | --------- |
| [테스트명] | [원인] | [방안]    |

### 커버리지

| 영역        | 현재 | 목표    | 상태  |
| ----------- | ---- | ------- | ----- |
| lib/        | X%   | 90%     | ✅/❌ |
| components/ | X%   | 75%     | ✅/❌ |
| api/        | X%   | 85%     | ✅/❌ |
| **전체**    | X%   | **80%** | ✅/❌ |

### 권장 조치

- [ ] [미커버 영역 테스트 추가]
```

---

## 옵션

| 옵션         | 설명                    |
| ------------ | ----------------------- |
| `--coverage` | 커버리지 리포트 포함    |
| `--watch`    | 파일 변경 감시          |
| `--related`  | 변경 파일 관련 테스트만 |
| `--update`   | 스냅샷 업데이트         |
| `--verbose`  | 상세 출력               |

---

## 사용 예시

```bash
# 전체 테스트
/test

# 특정 모듈
/test auth
/test components/workout

# 커버리지 포함
/test --coverage

# 특정 테스트 파일
/test WorkoutCard.test.tsx

# 패턴 매칭
/test --testNamePattern="should render"
```

---

## 관련 명령어

| 상황         | 권장 명령어                     |
| ------------ | ------------------------------- |
| 구현 후 검증 | /qcode → /test                  |
| 테스트 작성  | /test (yiroom-test-writer 연동) |
| 전체 검증    | /qcheck (테스트 포함)           |

---

**Version**: 2.0 | **Updated**: 2026-01-19
