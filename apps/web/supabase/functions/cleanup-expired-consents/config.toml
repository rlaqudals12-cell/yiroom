# 폐기된 cleanup-expired-consents Edge Function의 방어 설정
# 실제 보유기간 정리는 /api/cron/cleanup-consents가 단독 소유한다.

[function]
name = "cleanup-expired-consents"
verify_jwt = true

# 운영에서는 안전한 410 버전을 먼저 배포한 뒤 pg_cron을 해제하고 함수를 삭제한다.
